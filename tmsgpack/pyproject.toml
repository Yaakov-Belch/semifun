[project]
name = "tmsgpack"
version = "0.1.0"
description = "Typed MessagePack serializer (inspired by but different from msgpack)"
requires-python = ">=3.14"
license = {text = "ISC"}
authors = [
    { name = "Yaakov Belch", email = "yaakov.belch@gmail.com" },
]
dependencies = [
    "xxhash",
    "yb-tools>=0.1.0",
]

[tool.uv.sources]
yb-tools = { workspace = true }

[project.urls]
Source = "https://github.com/Yaakov-Belch/yb-tools"
Tracker = "https://github.com/Yaakov-Belch/yb-tools/issues"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "pytest-timeout>=2.3.0",
]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"
asyncio_mode = "auto"
timeout = 60
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=75.0", "Cython>=3.0,<4"]
build-backend = "setuptools.build_meta"
